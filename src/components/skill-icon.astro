---
import Popover from "./popover.astro";

interface Props {
	name: string;
	icon: string;
}

const { name, icon } = Astro.props;
---

<Popover title={name} placement="top">
	<span class={`${icon} h-4 w-4 flex items-center justify-center`}></span>
</Popover>
