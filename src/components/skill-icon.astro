---
import Popover from "./popover.astro";

interface Props {
	name: string;
	icon: string;
}

const { name, icon } = Astro.props;
---

<Popover title={name} placement="top">
	<span class={`${icon} h-5 w-5 flex items-center justify-center`}></span>
</Popover>
